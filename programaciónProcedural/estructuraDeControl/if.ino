/* 
  IF: Si lógico.
  ELSE: Si nó lógico.
*/

//"SI" LOGICO SIMPLE
  if( /* condicion */ ){
    /* codigo a ejecutar si la condicion es verdadera */
  }


//"SI" Y "SI NO" LOGICO
  if( /* condicion */ ){
    /* codigo a ejecutar si la condicion es verdadera */
  } else {
    /* codigo a ejecutar si la condicion es FALSA */
  }


//"SI", "SI, SI NO" Y "SI NO" LOGICOS
  if( /* condicion */ ){
    /* codigo a ejecutar si la condicion es verdadera */
  } else if( /* segunda posible condicion */ ){
    /* codigo a ejecutar si la condicion logica anterior fue falsa y esta otra condicion si es verdadera */
  } else if( /* tercera posible condicion */ ){
    /* codigo a ejecutar si la condicion anterior fue falsa y esta otra condicion si es verdadera */
  } else {
    /* codigo a ejecutar si todas las condiciones anteriores terminaron siendo falsas */
  }
